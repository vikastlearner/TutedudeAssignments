# This is Assignmet 8
<br>This is basic login user or registering the user. 
<br>Basically there are three pages - login, register and userdetails. 
<br>When a new user goes to path 127.0.0.1:5000/, they will see login page with option to register if new user. 
<br>Once user is registered, you can login with credentials userid and password. 
<br>Flow: 
    register: Register form >> Save data to DB >> Fetch data from DB based on user details >> Present on same page
    login: Fetch data from DB based on user details >> Present on same page

